# AI Process Bottleneck — pinned runtime dependencies (Milestone 5).
# Versions pinned to the verified working set for reproducible builds.
# NOTE: `ollama` is intentionally omitted — it is imported behind a
# try/except in src/genai/llm_router.py and the app falls back to
# OpenAI / offline mode when it is absent.

# --- Web / API ---
fastapi==0.121.2
uvicorn==0.38.0
starlette==0.49.3
python-multipart==0.0.20
slowapi==0.1.9

# --- Data validation / settings ---
pydantic==2.12.4

# --- Database / migrations ---
sqlalchemy==2.0.45
alembic==1.18.1
psycopg2-binary==2.9.11
pgvector==0.4.2

# --- Config / env ---
python-dotenv==1.2.1
PyYAML==6.0.3

# --- LLM / RAG / embeddings ---
openai==2.8.0
qdrant-client==1.18.0
sentence-transformers==5.4.1
transformers==5.7.0
huggingface-hub==1.10.1
tiktoken==0.12.0
tavily-python==0.7.24
torch==2.11.0

# --- ML / numerics ---
numpy==2.3.4
scikit-learn==1.7.2
pandas==2.3.3

# --- Auth / security helpers ---
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1
email-validator==2.3.0

# --- HTTP clients ---
aiohttp==3.13.2
httpx==0.28.1
